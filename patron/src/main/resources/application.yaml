spring:
  application:
    name: Patron
  cloud:
    stream:
      bindings:
        chairConsumer-in-0:
          destination: airplanes
      function:
        definition: requestHandler
server:
  port: 9002
management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - prometheus
---
spring:
  config:
    activate:
      on-profile: different-profile
server:
  port: 9003
